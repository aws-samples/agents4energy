# Secrets (local environment files are never committed)
.env.local

# Auto-populated from AWS credentials at deploy time
aws-targets.json

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/

# CLI Internals
.cli/*

# Ephemeral Staging
.cache/*

# Exception: Commit the State
!.cli/deployed-state.json
