{
  "name": "agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "env -u INIT_CWD ./node_modules/.bin/agentcore deploy --yes",
    "destroy": "env -u INIT_CWD ./node_modules/.bin/agentcore remove all --yes"
  },
  "devDependencies": {
    "@aws/agentcore": "0.18.0"
  }
}
